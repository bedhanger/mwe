[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "requiresthat"
version = "2025.6.10.1"
dependencies = []

[tool.pytest.ini_options]
pythonpath = "src"
