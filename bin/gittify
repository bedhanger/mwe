#!/usr/bin/env bash

# Attempt to make a dir a Git repository

A_DIR=${1:-.}

cd ${A_DIR} || ( echo "Can't get in..." ; exit -1 )

test -d .git || ( test -w . && git init && git add . && \
   git commit --allow-empty -m"Track $(realpath .)" )
